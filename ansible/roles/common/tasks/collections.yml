---
- name: install aws collection
  command: /usr/local/bin/ansible-galaxy collection install {{item}} --force-with-deps
  loop:
    -  amazon.aws
    -  community.aws
    -  community.docker:==v2.7.0
    


